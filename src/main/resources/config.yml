stack-output: ../stacks
type:
  include:
    start-on:
      - ru.exp
    contains:
      - filter
    synthetic: true
  exclude:
    contains:
      - ru.dev.flow.filters
method:
  exclude:
    equals:
      - toString